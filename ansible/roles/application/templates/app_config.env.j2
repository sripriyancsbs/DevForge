# ==============================================================================
# DevForge Application Runtime Configuration
# Generated and managed by Ansible Automation
# Idempotent baseline configuration for {{ app_name }} ({{ app_env }})
# ==============================================================================
DEVFORGE_APP_NAME={{ app_name }}
DEVFORGE_ENVIRONMENT={{ app_env }}
DEVFORGE_PORT={{ app_port | default(8000) }}
DEVFORGE_LOG_LEVEL={{ app_log_level | default('info') }}
DEVFORGE_MANAGED_BY={{ app_managed_by | default('ansible') }}
