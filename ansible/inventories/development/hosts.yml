# DevForge Local Development Inventory
all:
  children:
    development:
      hosts:
        devforge-local:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
